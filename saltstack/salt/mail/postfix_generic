{% for user in pillar['users'] -%}
{% if user.login in pillar['local_users'] and 'email' in user -%}
{{ user.login }}@{{ grains['host'] }}.localdomain {{ user.email }}
{%- endif %}
{%- endfor %}
@{{ grains['host'] }}.localdomain {{ pillar['sender_email'] }}
