# "-*- sh -*-"

export REPORTTIME=10

setopt shortloops

if ! [[ $OS =~ 'Windows' ]]; then
    limit coredumpsize 0
fi

setopt longlistjobs

autoload zmv
autoload -U zed

setopt equals

setopt noclobber
